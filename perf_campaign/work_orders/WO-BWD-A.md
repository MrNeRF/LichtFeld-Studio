Implementation engineer, LichtFeld-Studio. Dir: /home/gauss/projects/LichtFeld-Studio, branch lfs-elite (verify; NEVER checkout). Reads: perf_campaign/RULES.md, perf_campaign/DIRECTIVES-round2.md §Directive-1 (BWD-A only — the exact-math clamp; BWD-B/C/D are LATER orders, do not attempt).
TASK (rank-2 Wave 5): in blend_backward_cu (kernels_backward.cuh:439-704): after the s_last_contributor fill+sync (:504-519), block-reduce max of the 256 last_contributor entries; clamp the batch walk to T_eff = min(tile_n_primitives, max_contrib). Mathematically exact (skipped splats produce zero grad via the :592 gate). Also add the one-shot debug histogram of tile_n_primitives vs T_eff (print once ~iter 1700, behind env flag) to quantify tail waste.
TDD: fixed-seed bit-identical loss trajectory vs pre-change (exact math => byte-equal grads for surviving splats); kernel-time A/B via profile.sh late window (LFS_PROF_START=1600 LFS_PROF_STOP=1900 LFS_PROF_ITERS=1920) + bicycle window.
GATE: dual-workload; expect blend_backward −25-50% in saturated scenes. Commit with numbers + histogram evidence.

PRECONDITION — MAIN-CHECKOUT LOCK: before touching any file, acquire the main-checkout lock and hold it for your whole session: run all your work under `flock /home/gauss/lfs-campaign-out/main-checkout.lock <your commands>` or simply verify no other worker holds it (flock -n test; if busy, sleep 120 and retry). Only one worker may operate in /home/gauss/projects/LichtFeld-Studio at a time.
