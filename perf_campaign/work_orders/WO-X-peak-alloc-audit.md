Implementation engineer, LichtFeld-Studio. Dir: /home/gauss/projects/lfs-fleet/wt-V (worktree; branch: git switch -c lfs-elite-fX $(git rev-parse lfs-elite); verify). Run ./perf_campaign/setup-worktree.sh first. Reads: RULES.md (full-suite gate, build.sh, flock benches), PROGRESS.md Wave-4 flags 2+3.
TASK — restore two invariants that drifted in the Wave-4 merge:
1. **allocs/iter 0.05 -> 0.18**: instrument the alloc counter with per-site attribution (tag or log callsite on steady-state allocations after warmup; a debug env flag is fine), identify which merged path allocates ~0.13/iter (suspects: GT-cache bookkeeping, joint-codec bounds, densify workspace, W-in-progress paths), fix to grow-only/persistent. TDD: failing assertion test steady_allocs <= 0.06 first (fails at 0.18), then pass.
2. **ex-cache peak +256 MiB vs Wave 2** (bonsai 1194 ex-cache vs 938): use the VRAM ledger + pool breakdown to attribute the retained bytes to a subsystem/branch; eliminate or justify each (justified = documented in the ledger with an owner, like the GT cache). TDD: ledger-based peak assertion.
GATE: dual-workload bench — allocs/iter <= 0.06, ex-cache peak within +5% of Wave-2's 938 MiB (or each excess documented+ledgered), no speed regression, full-suite delta reported.
