Implementation engineer, LichtFeld-Studio. Dir: /home/gauss/projects/LichtFeld-Studio, branch lfs-elite (verify; NEVER checkout). Session under flock /home/gauss/lfs-campaign-out/main-checkout.lock (one worker in main checkout at a time).
Reads: perf_campaign/RULES.md, PROGRESS.md, plan Phase 2.1, worker G's landed SH-quant infrastructure (default OFF — find its commits/tests), FIX-2.2-directive.md (the register-pressure lessons apply: SH decode/encode must NOT recreate the live-range explosion — decode in registers at use sites, re-encode ONLY in the fused Adam tail AFTER the geometry backward per the F1 structure that WO-G2 establishes).
TASK — THE VRAM PRIZE (−102 B/splat, 409.4 -> ~307): complete the SH-rest 16-bit quantization wiring and flip it ON by default behind the existing runtime flag:
1. FastGS forward+backward shN readers decode-in-registers; single re-encode site in fused Adam (respect WO-G2's F1 kernel structure — coordinate: if G2 has not landed when you start, do G2's F1 reorder first or wait for it; check the queue state).
2. Every densify shN touchpoint (LAS children gather/scatter, free-slot fill, compact, MCMC relocate staging), gsplat path (or explicit dequant fallback), PLY/checkpoint export dequantized fp32 bit-compat, viewer mirror path.
3. Ledger: B/splat must report ~307 with the flag on; also implement G's 2.4 suggestion (drop shN float4 tail pad) if it benches neutral -> ~301.
TDD: codec roundtrip; render-equivalence PSNR > 55dB vs fp32 on synthetic; densify roundtrip tests; ledger fail-first.
GATE (strictest of the campaign): dual-workload; bicycle 7k A/B flag-on-vs-off loss CURVES overlapping + final within noise; bonsai LATE-WINDOW steady_ms no worse than flag-off (profile.sh, LFS_PROF_START=1600); B/splat <= 307. If quality fails: bounds granularity 128 before conceding; if speed fails: apply FIX-2.2 lessons. Flag stays ON only if ALL gates pass. Commit per step with numbers.
