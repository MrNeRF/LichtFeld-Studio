Implementation engineer, LichtFeld-Studio. Dir: /home/gauss/projects/LichtFeld-Studio, branch lfs-elite (verify; NEVER checkout).
MANDATORY reads: perf_campaign/RULES.md (build via ./perf_campaign/build.sh ONLY; dual-workload gate; bicycle = canary, compare LOSS CURVES), perf_campaign/BASELINE.md, perf_campaign/PROGRESS.md (Wave-2 numbers are your reference), SPEED_VRAM_OPTIMIZATION_PLAN.md Phase 2, and docs/analysis/spirulae-comparison/spirulae-quant.md + footprint-compare.md (codec designs).

WORK ORDER — Phase 2 quantization. THE quality-sensitive wave: strict TDD, strict bicycle loss-curve gates, runtime fallback flags for both codecs. Order: 2.2 first (smaller, self-contained), then 2.1 (the big one).

## Task 2.2 — Joint (u, log_s) Adam codec
Replace uint8 moments + per-primitive fp32 scales (adam_optimizer.hpp:45-54: exp_avg u8 + exp_avg_sq u8 + 2 fp32 scales/primitive) with the spirulae-style joint codec: per cell store linearly-quantized (u = m/(sqrt(v)+eps), log_s = log1p(sqrt(v)/eps)) at 16-bit non-SH / 8-bit SH, float4 bounds per 256-splat block, endpoint-exact, 0<->0 fixed point (all-zero state decodes to zero moments — free init). Decode+update+encode inside the existing fused Adam site (kernels_backward.cuh adam_step_row + SH path) — single writer. Densify/relocate/free-slot zeroing paths must reset the new state correctly (zero bounds+cells; check mrnf/mcmc reset_optimizer_state / relocate paths). Keep the OLD codec behind a runtime flag (env or param) for A/B and fallback. Saves ~26 B/splat (172->~146); ledger test must reflect it.
TDD: codec unit tests (encode/decode roundtrip error bounds, zero fixed point, bounds reduction); optimizer-step equivalence on synthetic data (quantized-Adam trajectory vs fp32 Adam reference within tolerance; compare against the OLD codec's error too — new must be <= old); ledger test updated (fail first with old sizes). GATE: dual-workload; bicycle 7k loss CURVE within baseline band + bonsai/bicycle final quality no worse than Wave-2 reference; B/splat drops ~429->~403 in bench output.

## Task 2.1 — SH-rest 16-bit block quantization
Store shN packed 2 B/cell (uint16) + float2 bounds per 256-splat block (per-splat-block layout like spirulae FPBO: bounds indexed by splat block, all SH cells of a splat share the block's bound — see spirulae-quant.md §1.3/2.5). Decode in registers wherever shN is read (fastgs forward SH evaluation + backward; find all readers of the swizzled shN buffer incl. gsplat path, viewer mirror, PLY export — export/checkpoint must dequantize to fp32 for bit-compat). Re-encode ONLY in the fused Adam kernel after the update (single writer; block min/max reduction for bounds). Adam moments for shN ride the 2.2 codec. Densify paths (LAS children shN gather/scatter, free-slot fill, compact, MCMC relocate staging) must read/write quantized form correctly — audit every shN touchpoint (grep shN_swizzled). Keep fp32 path behind the same runtime flag. Saves ~102 B/splat.
TDD: codec tests; render-equivalence test (PSNR of quantized-SH render vs fp32 render on synthetic scene > 55dB); densify-path roundtrip tests; ledger (fail-first). GATE: dual-workload with bicycle loss curves + a FULL bicycle 7k A/B (flag on vs off): final loss delta < noise band, curve overlap; B/splat ~403->~301. If quality regresses: investigate bounds granularity (128 vs 256) before conceding.

Commit per task with numbers. If any gate fails and cannot be fixed: leave the flag OFF by default, document in ISSUES.md, still land the infrastructure. Summary table at end.

NOTE — RESUME SEMANTICS: if your branch already contains commits for this order or the
working tree has uncommitted edits, that is a PREVIOUS INTERRUPTED ATTEMPT of this same
order. git log + git diff first; keep what is sound, finish or revert per-file; never
blindly restart from scratch and never discard committed work.
